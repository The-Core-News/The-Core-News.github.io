The editorial brief JSON and the URL to fetch appear to be missing from your message — the content after "Your editorial brief (JSON):" is empty, and the title format instruction is also cut off.

Could you share the editorial brief JSON (which should include the URL and topic details)?