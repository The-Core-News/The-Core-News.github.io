Your message appears to be incomplete — the editorial brief JSON and the URL to fetch are both missing. Could you share:  1. The **editorial brief JSON** (article topic, angle, target length, etc.) 2. The **source URL** to fetch and verify