메시지가 불완전하게 전달된 것 같습니다. 다음 두 가지가 누락되어 있습니다:

1. **Editorial brief (JSON)** — `Your editorial brief (JSON):` 이후 내용이 없습니다.
2. **URL** — "Fetch the URL above with WebFetch" 라고 되어 있지만 URL이 포함되지 않았습니다.

브리핑 JSON과 대상 URL을 다시 보내주시면 바로 작성하겠습니다.